# Base URL of the PrivyQ FastAPI gateway (ARCH §20).
# The browser talks to this directly, so it must be reachable from the client.
# Defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_URL=http://localhost:8000
